import DefaultLayout from "../../layouts/DefaultLayout"

const BrgyProfileSettings = () => {
    const Layout = () => (
        <div>
            This is Staff Profile Settings
        </div>
    )

    return <DefaultLayout component={<Layout/>} />
}

export default BrgyProfileSettings